keycloak:
  realm: Evaluation
  domain: localhost:8081
  adminClientId: admin-cli
  adminClientSecret: qu49USnFCthr7KMX3a49QLsl3XfFHJri
  client: authenticationClientId
  urls:
    auth: http://${keycloak.domain}

server:
  port: 9094
  servlet:
    context-path: /api/integration

spring:
  mail:
    host: smtp.gmail.com
    username: marwenbelakhel333@gmail.com
    password: nasx ctwu ldln sbcx
    port: 587
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true

  application:
    name: Micro-service-User

  cloud:
    discovery:
      enabled: true
    eureka:
      instance:
        prefer-ip-address: true
      client:
        service-url:
          defaultZone: ${DSCOVERY_URL:http://localhost:8761/eureka}

    management:
      endpoints:
        web:
          exposure:
            include: refresh

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost:3377/User_Service_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver


